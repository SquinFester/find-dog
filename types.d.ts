type DogSpec = {
  breed: string;
  ext?: string;
};

type DogImgPromise = {
  message: string;
  status: string;
};

type DogImg = {
  src: string[];
  name: string;
};

type DogOption = {
  name: string;
};
